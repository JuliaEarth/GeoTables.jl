load(args...; kwargs...) = error("the `load` function has been moved to the GeoIO.jl package")
save(args...; kwargs...) = error("the `save` function has been moved to the GeoIO.jl package")
gadm(args...; kwargs...) = error("the `gadm` function has been moved to the GeoIO.jl package")
